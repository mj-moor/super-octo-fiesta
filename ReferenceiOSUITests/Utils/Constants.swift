import Foundation

enum TestConstants {
    
    // MARK: - Timeouts
    enum Timeout {
        static let standard: TimeInterval = 10.0
        static let short: TimeInterval = 1.0
        static let long: TimeInterval = 30.0
        static let animation: TimeInterval = 0.5
    }
}
